/* This class represents a logging exception */
class LoggerException {
  constructor( message ) {
    this.name = 'LoggerException';
    this.message = message;
  }
}

export default LoggerException;
