{
  "name": "gennyjs",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rm -rf dist && mkdir dist",
    "build-src": "babel src --out-dir dist --source-maps inline --copy-files",
    "build": "npm run clean && npm run build-src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/genny-project/gennyjs.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/genny-project/gennyjs/issues"
  },
  "homepage": "https://github.com/genny-project/gennyjs#readme",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "eslint": "^4.4.1",
    "eslint-plugin-import": "^2.7.0"
  }
}